{
  "name": "magictools",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "coverage": "turbo run coverage",
    "lint": "eslint .",
    "test:infra": "node --test infra/scripts/lib/*.test.mjs",
    "smoke": "node infra/scripts/smoke.mjs",
    "qa:gate": "eslint . && turbo run build test && pnpm coverage && pnpm test:infra && pnpm docs:lint",
    "new:app": "node infra/scripts/new-app.mjs",
    "docs:lint": "markdownlint-cli2 \"docs/**/*.md\"",
    "changeset": "changeset",
    "release": "changeset version",
    "ws:create": "node infra/scripts/workspace.mjs create",
    "ws:cleanup": "node infra/scripts/workspace.mjs cleanup"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@vitest/coverage-v8": "^2",
    "eslint": "^9",
    "eslint-plugin-react-hooks": "^5",
    "markdownlint-cli2": "^0.14.0",
    "turbo": "^2.1.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8",
    "yaml": "^2.5.0"
  }
}
