{
  "name": "@mt/e2e",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "e2e:update": "playwright test --update-snapshots",
    "visual": "playwright test _visual.spec.ts",
    "visual:update": "playwright test _visual.spec.ts --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
