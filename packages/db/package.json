{
  "name": "@mt/db",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test:integration": "node ../../infra/scripts/test-database.mjs --project db"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "@mt/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/pg": "^8.11.0",
    "@types/node": "^20.14.0"
  },
  "files": [
    "dist",
    "migrations"
  ]
}
