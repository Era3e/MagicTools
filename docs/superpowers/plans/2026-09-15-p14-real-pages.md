# P14 实施计划

1. 先写 Assistant URL 装载、键盘激活和长上下文桩回答的失败测试。
2. 实现 `ChatPage` 历史会话参数与键盘语义，扩展 Assistant 桩回答。
3. 新增幂等业务样板 fixture 和键盘 E2E。
4. 扩展视觉页面映射、核心文案断言与动态路由填充，重生成 Windows 基线。
5. 跑目标单测、键盘 E2E、视觉、响应式与全量功能 E2E。
6. 更新 changeset、CHANGELOG、CODE_WIKI、coverage matrix 与即时记忆。
7. 本地 `qa:gate` 通过后提交 PR，等待 CI 三段绿并合并；main 合入后触发 Linux 基线更新。
